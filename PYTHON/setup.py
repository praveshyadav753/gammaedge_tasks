from setuptools import find_packages, setup
import csv
setup(
    name='customlib',
    packages=find_packages(include=['customlib']),
    version='0.1.0',
    description='My first Python library',
    author='Me',
    install_requires=[],

)

